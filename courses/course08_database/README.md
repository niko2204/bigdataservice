# Course 08. 데이터베이스 구축

분석용 테이블을 설계하고 CSV를 SQLite에 적재한다. 기본키, 자료형, 인덱스, SQL 집계를 학습하며 `python database/load_data.py`로 DB를 생성한다.

`lecture_note.md`의 스키마·제약조건·SQL 예제를 따라한 뒤 기존 단일 테이블을 지역, 관측, 점포 구조로 확장해 본다. SQL 결과는 pandas 계산과 대조한다.
